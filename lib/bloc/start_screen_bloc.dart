import 'package:flutter_bloc/flutter_bloc.dart';

class StartScreenBloc extends Bloc {
  StartScreenBloc(initialState) : super(initialState);

  String validation(String text, String keyWord) {
    return '';
  }
}
